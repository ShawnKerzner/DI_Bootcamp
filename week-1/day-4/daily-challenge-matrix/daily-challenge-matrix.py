def string_to_2D_list(coded_string):
    pass
# takes the matrix string, returns a 2D list


def alpha_characters(the_list):
    pass
# takes a list, returns a string of just the alpha characters


def symbol_to_spaces(the_list):
    pass
# takes a list, returns a list with symbol runs(between letters) replaced with spaces


def decode(string):
    pass
# takes the raw matrix string, returns the final decoded message, using the functions above
